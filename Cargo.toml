[package]
name = "sqdist"
version = "0.6.0"
edition = "2021"
description = "Fast string-distance CLI for typosquatting and homoglyph detection (Levenshtein, Damerau-Levenshtein, UTS#39 confusable-weighted)."
license = "MIT OR Apache-2.0"
repository = "https://github.com/ericfitz/sqdist"
homepage = "https://github.com/ericfitz/sqdist"
readme = "README.md"
keywords = ["typosquatting", "homoglyph", "levenshtein", "security", "unicode"]
categories = ["command-line-utilities", "text-processing"]
# Publish only what the crate needs — exclude repo tooling, design docs, and the
# release/signing scripts.
include = [
    "/build.rs",
    "/src/**/*.rs",
    "/Cargo.toml",
    "/README.md",
    "/LICENSE-MIT",
    "/LICENSE-APACHE",
    "/scripts/gen_confusables.py",
    "/scripts/gen_flowcrypt.py",
]

[[bin]]
name = "sqdist"
path = "src/main.rs"

[dependencies]
unicode-security = "0.1.2"
# Heap profiler, off by default: cargo build --release --features dhat-heap
dhat = { version = "0.3", optional = true }

[features]
dhat-heap = ["dep:dhat"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"
